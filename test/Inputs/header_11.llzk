// Imported via `call_with_affinemap_fail.llzk`

module attributes {llzk.lang} {
  struct.def @D0 {
    function.def @compute() -> !struct.type<@D0> {
      %self = struct.new : !struct.type<@D0>
      function.return %self: !struct.type<@D0>
    }
    function.def @constrain(%self: !struct.type<@D0>) {
      function.return
    }
  }
  poly.template @TD1 {
    poly.param @M

    struct.def @D1 {
      function.def @compute() -> !struct.type<@TD1::@D1<[@M]>> {
        %self = struct.new : !struct.type<@TD1::@D1<[@M]>>
        function.return %self: !struct.type<@TD1::@D1<[@M]>>
      }
      function.def @constrain(%self: !struct.type<@TD1::@D1<[@M]>>) {
        function.return
      }
    }
  }
  poly.template @TD2 {
    poly.param @M
    poly.param @N

    struct.def @D2 {
      function.def @compute() -> !struct.type<@TD2::@D2<[@M, @N]>> {
        %self = struct.new : !struct.type<@TD2::@D2<[@M, @N]>>
        function.return %self: !struct.type<@TD2::@D2<[@M, @N]>>
      }
      function.def @constrain(%self: !struct.type<@TD2::@D2<[@M, @N]>>) {
        function.return
      }
    }
  }
}
